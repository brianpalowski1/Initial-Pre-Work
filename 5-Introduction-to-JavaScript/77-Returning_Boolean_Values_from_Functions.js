// In this section you can simplify a function with simply using a return with a comparison

function isLess(a, b) {
  // Fix this code
  return a < b;
}

// Change these values to test
isLess(10, 15);
