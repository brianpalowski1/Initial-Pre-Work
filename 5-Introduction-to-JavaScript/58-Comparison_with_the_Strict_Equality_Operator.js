// this section goes over the strict equality operator which does not perform a type conversion just checks if they are equal.

// Setup
function testStrict(val) {
  if (val === 7) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testStrict(10);
