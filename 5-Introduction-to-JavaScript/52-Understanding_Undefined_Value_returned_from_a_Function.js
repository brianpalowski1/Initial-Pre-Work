// In this section a function does not need to have a return statement. In this case we are only chaning the global var when run.

// Example
var sum = 0;
function addThree() {
  sum = sum + 3;
}

// Only change code below this line

function addFive(){
  sum = sum + 5
}

// Only change code above this line
var returnedValue = addFive();
